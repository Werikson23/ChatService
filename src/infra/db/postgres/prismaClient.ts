//import { PrismaClient } from '@prisma/client';

//export const prisma = new PrismaClient();

//prisma.$connect()
//  .then(() => console.log('🟢 Prisma conectado ao Postgres'))
 // .catch((err) => {
 //   console.error('❌ Falha ao conectar ao Postgres', err);
 //   process.exit(1);
 // });
